# AVL Tree Implementation in Java

Implemented a self-balancing AVL Tree in Java. It supports insertion, deletion, and search. The tree automatically balances itself using rotations to ensure O(log n) tree operations.
